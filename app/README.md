# App — v2 playbook UI

Status: **deferred until taxonomy.yaml approved** (First Task step 4 gates this).

Plan: single-page, dependency-light (plain HTML + vanilla JS or single-file React).
Tabs: Taxonomy · Plays · Triggers · Scoring · Accounts.
Renders from `/data/*.yaml` — zero hardcoded content (CLAUDE.md §8 rule 1).
