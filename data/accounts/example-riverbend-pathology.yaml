# EXAMPLE ACCOUNT — FICTIONAL. Demonstrates lite card schema + scoring.
# All evidence confidence D (inference) by design. Do not pursue.
fictional: true

company: Riverbend Health System        # fictional
facility: Riverbend Central — Digital Pathology Lab
segment: Hospital / Health System
software:
  domain: Digital Pathology (WSI scanning + AI-assisted primary diagnosis)
  vendor: null                      # TODO: vendors.yaml entry when real
deployment: on-premises
operator: customer-operated             # → GTM motion: direct
trigger:
  id: pathology-golive                  # not in triggers.yaml — example-local
  detail: "Announced go-live of digital pathology primary diagnosis, 2 campuses"
  window: "Scanner fleet installed; archive + inference infra buy expected within 2 quarters"
infrastructure_control: "Y"
evidence:
  - claim: "Runs pathology scanners producing ~1.5 TB/day WSI"
    source: "Inference from announced scanner count x typical WSI output"
    confidence: D
    todo: true
  - claim: "IT operates own datacenter, VMware-based"
    source: "Inference from health-system size + job posting pattern"
    confidence: D
    todo: true
  - claim: "Evaluating AI-assisted diagnosis, needs GPU inference on-prem (PHI)"
    source: "Inference from digital pathology primary-diagnosis announcement"
    confidence: D
    todo: true
next_step: "Verify infra operator + scanner fleet via intro call; upgrade evidence to B/C"

# Play mapping
play: play-a

# Regulatory flags (cross-cutting, §3)
regulatory:
  phi: true
  gxp: false
  part11: false
  samd: true          # if AI algorithm used for primary diagnosis
  notes: "SaMD flag pending confirmation of algorithm deployment mode"

# ---- scoring: 8 items, 0-5 (keys from data/scoring.yaml) ----
scoring:
  infrastructure_control: 5              # customer-operated on-prem (D — verify first)
  compute_storage_intensity: 4  # ~1.5 TB/day WSI + GPU inference
  refresh_trigger: 4            # go-live announced, infra buy window open
  mission_criticality: 4        # primary diagnosis = clinical production
  data_growth_retention: 4      # WSI archive grows PB-scale, 10yr+ retention
  multi_site_repeatability: 3   # 2 campuses now, health system has 5
  partner_access: 2             # pathology ISV relationship unknown
  competitive_differentiation: 3 # incumbent server vendor unknown
